{
  "name": "@captain-food/codegen",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Deterministic code/doc generator: reads the specs/*.yaml model, validates referential integrity, and emits derived artifacts (traceability, GraphQL, SQL, types...).",
  "bin": {
    "captain-codegen": "src/cli.ts"
  },
  "scripts": {
    "validate": "tsx src/cli.ts --check",
    "generate": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "yaml": "^2.6.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
